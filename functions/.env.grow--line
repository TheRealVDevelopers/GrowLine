# Runtime config for the Cloud Functions when deployed to the grow--line project.
# Committed on purpose: it holds no secret, only the app's public address.
# See .env.example in this directory for why the seven HTTP-calling jobs are
# silent no-ops without it.
SITE_URL=https://growline--grow--line.asia-east1.hosted.app
